# Hyperliquid Asset Catalog

A production-oriented, read-only Python catalog for native perpetuals, dynamically discovered
HIP-3 DEXes and spot markets on Hyperliquid. It uses the public Info API and contains no wallet,
signing or order path.

Documentation versions follow package releases. The top navigation covers the CLI, stable data
contracts, analytics methodology, deployment and contribution workflow.
